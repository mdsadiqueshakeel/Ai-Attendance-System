server:
  port: 8080

spring:
  datasource:
    # Supabase uses SSL; include sslmode=require in your JDBC URL.
    # Example:
    # jdbc:postgresql://db.xyljztuoplzrrwdizjxh.supabase.co:5432/postgres?sslmode=require
    url: ${DB_URL:jdbc:postgresql://localhost:5432/postgres}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  jwt:
    # Use a long random secret in production, passed via env var.
    secret: ${JWT_SECRET:dev-change-me}
    issuer: smart-attendance
    access-token-ttl-minutes: 60
  upload:
    dir: ${UPLOAD_DIR:uploads}

